name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  completions:
    name: Generate shell completions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        run: rustup target add x86_64-unknown-linux-gnu
      - name: Build
        run: cargo build --release
      - name: Generate completions
        run: |
          mkdir -p completions
          ./target/release/mdr --completions bash > completions/mdr.bash
          ./target/release/mdr --completions zsh > completions/mdr.zsh
          ./target/release/mdr --completions fish > completions/mdr.fish
      - name: Upload completions
        uses: actions/upload-artifact@v4
        with:
          name: completions
          path: completions/

  build:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            ext: ""
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
            ext: ""
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-13
            ext: ""
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            ext: ""
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            ext: .exe
            archive: zip

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        run: rustup target add ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Prepare archive
        shell: bash
        run: |
          BIN="target/${{ matrix.target }}/release/mdr${{ matrix.ext }}"
          ARCHIVE_NAME="mdr${{ matrix.ext }}"
          cp "$BIN" "$ARCHIVE_NAME"
          PKG="mdr-${{ matrix.target }}"
          if [ "${{ matrix.archive }}" = "tar.gz" ]; then
            tar czf "$PKG.tar.gz" "$ARCHIVE_NAME"
            echo "ARTIFACT=$PKG.tar.gz" >> $GITHUB_ENV
          else
            zip "$PKG.zip" "$ARCHIVE_NAME"
            echo "ARTIFACT=$PKG.zip" >> $GITHUB_ENV
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mdr-${{ matrix.target }}
          path: ${{ env.ARTIFACT }}

  release:
    needs: [build, completions]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: mdr-*
          merge-multiple: true

      - uses: actions/download-artifact@v4
        with:
          name: completions
          path: completions/

      - name: Generate release body
        run: |
          cat > notes.md <<'EOF'
          ## mdr

          ### Downloads

          | Architecture | File |
          |---|---|
          EOF
          for f in *.*; do
            [[ "$f" == *.md ]] && continue
            [[ "$f" == *.txt ]] && continue
            [ -f "$f" ] || continue
            arch=$(echo "$f" | sed 's/^mdr-//; s/\.tar\.gz$//; s/\.zip$//')
            echo "| \`$arch\` | \`$f\` |" >> notes.md
          done
          echo "" >> notes.md
          echo "### Shell completions" >> notes.md
          echo "" >> notes.md
          echo "Available in the release assets:" >> notes.md
          echo "- \`completions/mdr.bash\` — Bash" >> notes.md
          echo "- \`completions/mdr.zsh\` — Zsh" >> notes.md
          echo "- \`completions/mdr.fish\` — Fish" >> notes.md

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: mdr ${{ github.ref_name }}
          body_path: notes.md
          files: |
            *.tar.gz
            *.zip
            completions/mdr.bash
            completions/mdr.zsh
            completions/mdr.fish
          generate_release_notes: true